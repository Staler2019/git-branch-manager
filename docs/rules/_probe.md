# Import probe (temporary)

This file exists to verify that CLAUDE.md's `@import` mechanism really loads a
file from `docs/rules/`. It is deleted in the commit right after the check.

## [PROBE-import-works] The import probe

- **Rule**: if you can quote this line, `@docs/rules/_probe.md` resolved and the
  rules split is safe to continue.
- **Evidence**: this round; deleted once confirmed.
